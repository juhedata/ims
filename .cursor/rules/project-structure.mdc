---
description: 项目目录与技术栈总览（backend FastAPI / frontend Vue）
alwaysApply: true
---

# 项目结构

本仓库为前后端分离的库存管理系统（IMS）。

| 目录 | 角色 | 技术栈 |
|------|------|--------|
| `backend/` | Python 后端 API 服务 | FastAPI、SQLAlchemy、Alembic、Pydantic、uvicorn |
| `frontend/` | 调用后端的前端项目 | Vue 3、Vite、Vue Router、Pinia、Element Plus、axios |

## 约定

- 改 API、模型、迁移、业务逻辑 → 在 `backend/`（入口多为 `backend/main.py`，应用代码在 `backend/app/`）
- 改页面、路由、状态、UI → 在 `frontend/`（源码在 `frontend/src/`）
- 前端通过 HTTP 调用后端 API；联调时保持接口路径、请求/响应字段与后端 `schemas` / 路由一致
- 后端 Python ≥ 3.12；前端 Node ≥ 20
